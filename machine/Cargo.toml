[package]
name = "netsim-embed-machine"
version = "0.1.0"
authors = ["David Craven <david@craven.ch>"]
edition = "2018"
description = "Runs a future in a separate network namespace with a TUN adapter."
license = "MIT"
repository = "https://github.com/ipfs-rust/netsim-embed"

#[features]
#tokio2 = ["mio", "tokio"]

[dependencies]
futures = "0.3.12"
ioctl-sys = "0.6.0"
libc = "0.2.82"
log = "0.4.13"
#mio = { version = "0.7.7", optional = true }
netsim-embed-core = { version = "0.1.0", path = "../core" }
smol = "1.2.5"
#tokio = { version = "1.0.2", features = ["io-driver", "rt-threaded"], optional = true }
